# Forge Environment Configuration
# Copy this file to .env.local and fill in the values

# =============================================================================
# Build Configuration
# =============================================================================

# Base path for the application (used for deployment to subpaths)
# Examples:
#   VITE_BASE_PATH=/                     # Root deployment (default)
#   VITE_BASE_PATH=/forge/               # Subpath deployment
#   VITE_BASE_PATH=/api/hassio_ingress/xxx/  # Home Assistant ingress
# VITE_BASE_PATH=/

# =============================================================================
# Application Settings
# =============================================================================

# Enable debug logging (set to namespace patterns, e.g., "fs,store" or "*" for all)
# VITE_DEBUG=

# =============================================================================
# Feature Flags (optional)
# =============================================================================

# Enable experimental features
# VITE_ENABLE_EXPERIMENTAL=false

# =============================================================================
# Server Persistence (SQLite Backend)
# =============================================================================

# Use server-based persistence instead of IndexedDB
# When enabled, the app will connect to the Express server for data storage
# VITE_USE_SERVER_PERSISTENCE=true

# Server API URL (defaults to http://localhost:3000/api if not set)
# VITE_API_URL=http://localhost:3000/api

# =============================================================================
# Server-Side Configuration (Express Server)
# =============================================================================

# Port for the Express server (default: 8099)
# PORT=8099

# Directory for Forge project data (default: ./data in dev, /data in Docker)
# FORGE_DATA_DIR=/data

# Directory for static frontend files (default: ../dist)
# FORGE_STATIC_DIR=/app/dist

# Path to SQLite database file (default: FORGE_DATA_DIR/forge.sqlite)
# FORGE_DB_PATH=/data/forge.sqlite

# Tailscale hostname for CORS (allows requests from this hostname)
# Used when accessing via Tailscale direct URL instead of HA ingress
# Example: ha-green.tail12345.ts.net
# TAILSCALE_HOSTNAME=
